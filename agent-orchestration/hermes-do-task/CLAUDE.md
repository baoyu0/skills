# Do-Task Skill — Claude Code

When the user gives a non-trivial task, follow this workflow:

1. Plan (break down into sub-tasks)
2. Execute via sub-agents
3. Verify
4. Handoff

Simple tasks skip the workflow.
